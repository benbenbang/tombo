[project]
name = "test"
version = "0.1.0"
description = ""
authors = [{ name = "benbenbang", email = "bn@bitbrew.dev" }]
readme = "README.md"
requires-python = "~=3.11"
dependencies = [
    "loguru (>=0.7.3,<0.8.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "attrs (==25.3.0)",
    "fastapi",  # No version constraint
    "pydantic (?)",  # Version will be auto-filled
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.uv.workspace]
members = ["test"]
